/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/FirstEcommerceReact",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };

export default nextConfig;
